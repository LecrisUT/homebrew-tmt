# LecrisUT Tmt

## How do I install these formulae?

`brew install lecrisut/tmt/<formula>`

Or `brew tap lecrisut/tmt` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
